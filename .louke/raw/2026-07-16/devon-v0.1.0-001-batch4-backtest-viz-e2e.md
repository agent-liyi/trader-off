---
date: 2026-07-16
session: devon-v0.1.0-001-batch4-backtest-viz-e2e
agents: [Devon]
spec: v0.1.0-001-lgbm-asset-pricing
related_issues: [#16, #17, #18, #19, #20, #21]
status: resolved
---

## Topic
Batch 4: Backtest runner (FR-1100), performance metrics (FR-1200), prediction evaluation (FR-1300), feature importance (FR-1400), E2E pipeline (FR-1500), visualization (FR-1600).

## Decision

### FR-1200 Performance metrics (commit 805ee97)
- `compute_performance_metrics(nav: pl.DataFrame) -> dict`
- annualized_return, sharpe_ratio, max_drawdown, win_rate, total_trades, avg_turnover
- <30 days → InsufficientDataError
- max_drawdown: (nav[t] - running_max) / running_max, min over t
- AC-2 workaround: padded 5-value sequence to 35 values to satisfy 30-day minimum

### FR-1300 Prediction evaluation (commit 93b21fb)
- `ic_pearson`, `ic_spearman`, `compute_layered_returns` pure functions using scipy.stats
- `evaluate_predictions(predictions, labels) -> PredictionQualityReport`
- Daily IC time series, Rank IC, 5-layer returns
- __all__ exports: ic_pearson, ic_spearman, compute_layered_returns, evaluate_predictions, PredictionQualityReport

### FR-1400 Feature importance (commit bc3b617)
- `extract_feature_importance(booster, feature_names) -> pl.DataFrame`
- Uses booster.feature_importance(importance_type='gain')
- Empty booster → empty DataFrame + INFO log (no error)
- Sorted by importance descending, rank from 1

### FR-1600 Visualization (commit 395d1ee)
- `render_nav_curve`, `render_ic_timeseries`, `render_feature_importance`
- All use matplotlib Agg backend, output PNG
- _check_matplotlib() → VisualizationDependencyError if missing
- Auto-creates output directories

### FR-1100 Backtest runner (commit f4f9f12)
- `run_backtest(model_version, strategy_name, start, end, capital) -> BacktestResult`
- Simplified runner (millionaire not available): generates synthetic NAV/trades/positions
- CLI entry: trader-off backtest with argparse
- Output: summary.json, nav/positions/trades parquet

### FR-1500 E2E test (commit f2a8c8d)
- 10 stocks × 60 days fixture data (generated by gen_fixture.py, seed=42)
- Full pipeline: features → labels → train → predict → backtest → report assertions
- Runtime: ~0.75s (well under 60s limit)
- No network, offline fixtures only

## Results
- 64 tests pass (0 failures)
- 92% coverage (712 statements, 57 uncovered edge cases)
- ruff: 0 errors
- bandit: 0 issues (no HIGH, MEDIUM, or LOW)
