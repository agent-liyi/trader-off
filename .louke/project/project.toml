[project]
version = "v0.5.2"
repo = "github.com/agent-liyi/trader-off"
project = "trader-off-v0.5.2"
project_id = "https://github.com/users/agent-liyi/projects/13"
spec_id = "v0.5.2-001-factor-mining-cli-fix"
release_branch = "releases/v0.5.2"

[meta]
pre_commit = "installed (python + base)"
created = "2026-07-22"
current_stage = "M-FOUND"
security_audit = "enabled"
smoke_test_issue = "TBD"
smoke_test_pr = "TBD"
dod = "All e2e tests pass; Unit test coverage >=97%; Mutation testing score >=70% (mutmut); Performance budgets met (train<=300s predict<=5s backtest<=600s mem<=16GB); Documentation sync (architecture/API/ADRs); Security review (M-SECURITY)"
test_framework = "pytest"
backlog_project = "https://github.com/users/agent-liyi/projects/4"
acknowledged_orphan_releases = ["releases/v0.4.0", "releases/v0.5.0"]

[e2e]
paths = "['tests/e2e', 'tests/perf']"
run = "uv run pytest tests/e2e tests/perf -m e2e -v"

[integration]
paths = "['tests/integration']"
run = "uv run pytest tests/integration -m integration -v"
