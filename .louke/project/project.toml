[project]
version = "v0.4.0"
repo = "github.com/agent-liyi/trader-off"
project = "trader-off-v0.4.0"
project_id = "TODO"
spec_id = "v0.4.0-001-quantide-data-adapter"
release_branch = "releases/v0.4.0"

[meta]
pre_commit = "installed (python + base)"
created = "TODO"
current_stage = "M-FOUND"
security_audit = "enabled"
smoke_test_issue = "#105 (closed)"
smoke_test_pr = "#TODO (closed)"
dod = "All e2e tests pass; Unit test coverage >=97%; Mutation testing >=70%; Performance budgets met; Documentation synced; Security review (M-SECURITY)"
test_framework = "pytest"
backlog_project = "TODO"
